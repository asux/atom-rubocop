# Rubocop package for Atom editor

**NOTICE**: This is alpha version.

Package implements Rubocop commands in [Atom](http://atom.io). Currently implemented autocorrect for current file only.

## Installation

To install package clone this repo to atom packages:

```
cd ~/.atom/packages
git clone git://github.com/asux/atom-rubocop.git
```
